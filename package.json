{
  "name": "picoweb-eleventy",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "eleventy && touch _site/.nojekyll",
    "dev": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "js-yaml": "^4.1.0",
    "markdown-it": "^13.0.1"
  }
}
