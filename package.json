{
  "name": "picoflow-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "PicoFlow marketing site and documentation, built with Eleventy.",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy && node scripts/check.mjs",
    "check": "node scripts/check.mjs",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "highlight.js": "^11.11.1",
    "markdown-it-anchor": "^9.2.0"
  }
}
